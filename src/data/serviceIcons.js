import {
  BookOpen,
  Mic,
  GraduationCap,
  Award,
  Calendar,
  Users,
  Briefcase,
  PenTool,
  Presentation,
  MessageSquare,
  Target,
  Lightbulb,
} from "lucide-react";

/** Icon choices available for a service card — shared by the admin icon
 *  picker and the public Services section so both resolve the same names. */
export const SERVICE_ICONS = {
  BookOpen,
  Mic,
  GraduationCap,
  Award,
  Calendar,
  Users,
  Briefcase,
  PenTool,
  Presentation,
  MessageSquare,
  Target,
  Lightbulb,
};

export const SERVICE_ICON_NAMES = Object.keys(SERVICE_ICONS);

export const DEFAULT_SERVICE_ICON = "BookOpen";
